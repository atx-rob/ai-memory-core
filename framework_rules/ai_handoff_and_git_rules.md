*** AI HANDOFF INSTRUCTIONS ***

## STEP 1: Mandatory Environment Gate
Before reading any code or writing any files, you MUST run the following command:
.\ai_instructions\scripts\Invoke-Sync.ps1

If the script outputs [HALTED], provide the user with the exact fix commands shown in the output. 
You are NOT ALLOWED to proceed with Git operations or code generation until the script outputs [SYNC COMPLETE].

**ESCAPE HATCH:** If the user cannot fix the environment, or if the AI is stuck in a loop, the user will type "OVERRIDE". If you see "OVERRIDE", you must immediately drop the environment restriction and proceed with the requested task.

## STEP 2: Code Retrieval
Once the environment gate passes (or is overridden), use your web browsing tool to fetch the source code from the GitHub URLs listed in this document.

## STEP 3: Acknowledgment
Acknowledge that you understand the rules, the environment is verified (or overridden), and you are ready to begin.

--- END OF AI HANDOFF INSTRUCTIONS ---

##